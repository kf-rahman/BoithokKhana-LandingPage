# OS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
env/

# Node / Next.js
node_modules/

# Build-time copy of the root index.html (see frontend/scripts/sync-marketing.mjs).
# Root index.html is the source of truth; this copy must never be committed.
frontend/public/site.html
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Local databases (SQLite dev file — migrations are committed, the data is not)
*.db
*.sqlite3
*.db.bak

# Redundant factory staging artifacts (the factory is installed at the repo root)
family-catering-factory/
family-catering-factory.zip

# Env / secrets (never commit real values)
.env
.env*.local

# Logs
*.log
