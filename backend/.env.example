# Copy to `.env` and adjust as needed.
# `.env` is gitignored AND blocked by the pre-commit hook — never commit real secrets.

APP_NAME=Family Catering Order System API

# Added when the order parser is built:
# ANTHROPIC_API_KEY=
